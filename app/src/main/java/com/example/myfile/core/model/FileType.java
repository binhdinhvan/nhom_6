package com.example.myfile.core.model;

/**
 * Phan loai file o muc nghiep vu (khong phai MIME tho).
 * ViewerFactory (C) va IconMapper (C) route dua tren enum nay.
 */
public enum FileType {
    FOLDER,
    IMAGE,
    VIDEO,
    AUDIO,
    ARCHIVE,
    DOCUMENT,
    APK,
    TEXT,
    OTHER
}
